pull images

```bash
docker-compose pull
```


startup

```bash
docker-compose up -d
```

stop 

```bash
docker-compose stop
```

remove

```bash
docker-compose down
```

see status

```bash
docker ps -a
```

